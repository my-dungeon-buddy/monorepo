import React from "react";
import styled from 'styled-components';
import Button from '../Button'

const Form = styled.section`

`

function InitiativeTracker() {
    return (
        <main>
            <Form>
                <Button>teub</Button>
            </Form>
            <Form>
                <Button>tfzeeeub</Button>
            </Form>
            <section>

            </section>
        </main>
    )
}

export default InitiativeTracker