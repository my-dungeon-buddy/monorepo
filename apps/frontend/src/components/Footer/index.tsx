import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.footer`
  grid-area: footer;
  background: #555;
`;

export const Footer = () => (
  <Wrapper>
    footer
  </Wrapper>
);
